<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.edu.unifavip.layouts">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".RelativeLayoutActivity"
            android:label="@string/activity_relative_layout"
            android:theme="@style/AppTheme"/>

        <activity
            android:name=".LinearLayoutActivity"
            android:label="@string/activity_linear_layout"
            android:theme="@style/AppTheme"/>

        <activity
            android:name=".FrameLayoutActivity"
            android:label="@string/activity_frame_layout"
            android:theme="@style/AppTheme"/>

        <activity
            android:name=".ScrollViewActivity"
            android:label="@string/activity_scroll_view"
            android:theme="@style/AppTheme"/>

    </application>

</manifest>